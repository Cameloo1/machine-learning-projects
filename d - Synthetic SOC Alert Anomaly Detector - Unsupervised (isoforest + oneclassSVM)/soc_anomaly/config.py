"""Global configuration primitives for the SOC anomaly project."""

from __future__ import annotations

DEFAULT_RANDOM_STATE: int = 42
DEFAULT_N_USERS: int = 200
DEFAULT_EVENTS_PER_USER: int = 200
DEFAULT_ANOMALY_FRACTION: float = 0.01

